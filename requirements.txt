# RoleFit - AI Recruiter Brain
# Phase 1: Data ingestion & core models
pandas>=2.0.0
pydantic>=2.0.0
numpy>=1.24.0

# Phase 2: Retrieval + Phase 3: Re-ranking
# (installed together since sentence-transformers pulls torch anyway)
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
transformers>=4.30.0
torch>=2.0.0

# Phase 5: Sandbox demo (uncomment when needed)
streamlit>=1.30.0

# Testing
pytest>=7.0.0
